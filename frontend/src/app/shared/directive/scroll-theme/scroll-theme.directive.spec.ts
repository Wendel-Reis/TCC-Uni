import { ScrollThemeDirective } from './scroll-theme.directive';

describe('ScrollThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollThemeDirective();
    expect(directive).toBeTruthy();
  });
});
