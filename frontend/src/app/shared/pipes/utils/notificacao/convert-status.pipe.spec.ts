import { ConvertStatusPipe } from './convert-status.pipe';

describe('ConvertStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
