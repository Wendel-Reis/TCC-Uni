import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maestroapps.dls',
  appName: 'Dark Lion Solutions',
  webDir: 'www',
  bundledWebRuntime: false,
  
};

export default config;
